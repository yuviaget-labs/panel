{
  "name": "yuvi-panel",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute yuvi-panel-db --file=schema.sql",
    "db:init:local": "wrangler d1 execute yuvi-panel-db --local --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
